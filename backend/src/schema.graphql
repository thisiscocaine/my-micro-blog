type Mutation {
  signup(firstName: String!, lastName: String!, email: String!, password: String!): AuthPayload!
  # other mutations...
}

type AuthPayload {
  token: String
  user: User!
}

type User {
  id: ID!
  email: String!
  verified: Boolean!
  # ...
}
